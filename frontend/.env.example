# Copy to frontend/.env.local

# Where the browser sends API calls. Leave as-is to use the built-in Next.js
# rewrite proxy to the backend (recommended for local dev).
NEXT_PUBLIC_API_URL=/api

# Backend origin the Next.js dev server proxies /api/* to.
BACKEND_URL=http://localhost:8000
